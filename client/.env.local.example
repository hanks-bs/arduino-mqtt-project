# Public WebSocket base URL (reachable from the browser)
NEXT_PUBLIC_WS_URL=ws://localhost:5000

# Optional HTTP base for API calls (polling, toggles, CSV export)
NEXT_PUBLIC_API_BASE=http://localhost:5000